//var names = ['Sam', 'George', 'Fred', 'Roxy'];

//names.forEach(function (name){
  //console.log('forEach', name);
//});

//names.forEach((name) => {
  //console.log('arrowFunc', name); // arrow functions all multiple statements here
//});

//names.forEach((name) => console.log(name)); // for single statement much simpler

//var returnMe = (name) => name + '!';
//console.log(returnMe('Sammy'));

//var person = {
  //name: 'Samantha',
  //greet: function () {
    //names.forEach ((name)=> {
      //console.log(this.name + ' says hi to ' + name)
    //});
//  }
//};
